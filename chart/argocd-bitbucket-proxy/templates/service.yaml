apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-bitbucket-proxy.fullname" . }}
  labels:
    {{- include "argocd-bitbucket-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-bitbucket-proxy.selectorLabels" . | nindent 4 }}
